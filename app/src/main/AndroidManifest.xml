<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/bg_splash"
        android:label="@string/app_name"
        android:roundIcon="@drawable/bg_splash"
        android:supportsRtl="true"
        android:theme="@style/Theme.Entregaexpressa"
        tools:targetApi="31">
        <activity
            android:name=".presentation.SplashActivity"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.DeliveryListActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.DeliveryDetailsActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.DeliveryFormActivity"
            android:exported="true" />
    </application>

</manifest>